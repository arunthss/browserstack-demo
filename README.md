# demo selenium with pytest project
## execute pip install -r requirements.txt
## run pytest from root folder

# commandline args
- browser accepts chrome/firefox/edge
- count number of articles to scrape (default 5)
